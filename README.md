# Terminal To-Do List

A simple, command-line based to-do list manager written in Python.

## Features
- Add tasks
- View active tasks
- Mark tasks as finished
- View history with a calendar-like view
- Delete active tasks

## Getting Started

1. Set up your virtual environment if you haven't already.
2. Run the application:
   ```
   python todo.py
   ```

## Notes
- `todos.json` and `history.json` are stored in the `data/` folder for better organization.
- (You can add any extra notes for yourself here)
